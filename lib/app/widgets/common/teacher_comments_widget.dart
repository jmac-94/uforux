import 'package:flutter/material.dart';

class TeacherCommentsWidget extends StatelessWidget {
  const TeacherCommentsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'TeacherCommentsWidget',
      ),
    );
  }
}
